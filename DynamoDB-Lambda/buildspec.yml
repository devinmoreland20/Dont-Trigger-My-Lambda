version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
  post_build:
    commands:
      - echo Entered the post_build phase...
      - aws lambda update-function-code --function-name QueryTest --zip-file
